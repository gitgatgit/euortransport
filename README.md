# How to Run the R Script

To run `longR-improved-final.R`, use the following command:

```bash
Rscript longR-improved-final.R
```

## Requirements
- R must be installed on your system
- All required R packages must be installed (the script will indicate if any are missing)
- All input data files referenced by the script must be present in the same directory